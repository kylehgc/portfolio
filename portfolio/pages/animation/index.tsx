import { Center } from '@chakra-ui/react'
import IconAnimation from '../../Components/IconAnimation'

const Animation = () => {
	return (
		<>
			<Center h={'100vh'} w={'100vw'}>
				<IconAnimation setDone={() => {}} />
			</Center>
		</>
	)
}

export default Animation
